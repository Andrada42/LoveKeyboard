<Window x:Class="LoveKeyboard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoveKeyboard"
        mc:Ignorable="d"
        Title="LoveKeyboard" Height="150" Width="400"
        Background="Black"
        
        WindowStyle="None"
        AllowsTransparency="True"

        Loaded="Window_Loaded"
        ResizeMode="CanResize"
    >

    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F2D0BB"/>
            <Setter Property="Foreground" Value="#704123"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter 
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E5BFA6"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#D0A78F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TitleBarCloseButtonStyle" 
               BasedOn="{StaticResource TitleBarButtonStyle}" 
               TargetType="Button">
            <Setter Property="Foreground" Value="#704123"/>
            <Setter Property="Background" Value="#F2D0BB"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Red"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#990000"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CopyButtonStyle" TargetType="Button">
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
            <Setter Property="Background" Value="#F2D0BB"/>
            <Setter Property="BorderThickness" Value="0"/>
            
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter 
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F2D0BB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border
        Background="#F2D0BB"
        BorderBrush="#946C54"
        BorderThickness="2"
    >
        <DockPanel>
            <Grid DockPanel.Dock="Top" Height="30" Background="#F2D0BB">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="LoveKeyboard" Foreground="#704123" FontSize="16" FontWeight="bold" VerticalAlignment="Center"
                        Margin="10,0"/>
                    <!-- Minimize Button -->
                    <Button Grid.Column="1" Content="_" Style="{StaticResource TitleBarButtonStyle}" Width="30" Click="MinimizeButton_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <!-- Maximize/Restore Button -->
                    <Button Grid.Column="2" Content="□" Style="{StaticResource TitleBarButtonStyle}" Width="30" Click="MaximizeButton_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
                    <!-- Close Button -->
                    <Button Grid.Column="3" Content="X" Style="{StaticResource TitleBarCloseButtonStyle}" Width="30" Click="CloseButton_Click" WindowChrome.IsHitTestVisibleInChrome="True"/>
            </Grid>
            <Grid>
                <TextBox x:Name="MainTextBox"
                        TextChanged="MainTextBox_TextChanged"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Margin="15"
                        FontSize="20"
                        TextWrapping="Wrap"
                        AcceptsReturn="True"
                        VerticalScrollBarVisibility="Auto"
                        Background="#F2D0BB"
                        BorderThickness="0"
                        />
                    
                    <Button Click="CopyButton_Click"
                            Style="{StaticResource CopyButtonStyle}"
                            Width="50"
                            Height="50"
                            >
                        <Image Source="pack://application:,,,/Images/clipboard.png"/>
                    </Button>
                    

            </Grid>
        </DockPanel>
    </Border>
</Window>
